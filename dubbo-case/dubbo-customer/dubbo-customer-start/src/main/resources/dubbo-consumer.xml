<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans        
    http://www.springframework.org/schema/beans/spring-beans.xsd        
    http://code.alibabatech.com/schema/dubbo        
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 消费方应用名，用于计算依赖关系，不是匹配条件，不要与提供方一样,如果是消费方又是提供方,那就去年这行,在applicationContext.xml里引入dubbo-provider.xml文件,否则不用这个文件 -->
	<!-- 
	<dubbo:application name="dubbo-customer-business" />
	 -->
	
	<!-- 提供者服务系统,用来在消费服务里要调用提供者服务就要加上去，否则不用加 -->
	<dubbo:reference id="providerService" interface="com.hyf.openapi.interfaces.ApiInterface" timeout="10000" check="false" url="dubbo://127.0.0.1:80001"/>

</beans>